# Decision Memo: Calibrated WFH-Exposure Index & Cell-Based Primary DDD

**Status: DECIDED — statistically-calibrated occupation exposure, swapped into a pre-period demographic-cell shift-share measure, is the regressor of the employment DDD (the project's secondary specification since 2026-09-12; see the updates below).** See "Decision" at the bottom.

**Update (2026-09-12):** this memo's `Employed`-outcome DDD is now the project's **secondary** DDD specification — superseded in primacy by the hours-outcome DDD in `docs/decisions/hours-ddd-pivot.md`, per the project-wide pivot to the intensive margin (weekly work hours) as the primary dependent variable. Note the two DDDs use different exposure measures: this memo's cell-based shift-share measure (4) is the regressor for the secondary (`Employed`) DDD; the primary (hours) DDD instead uses the pure occupation-level calibrated measure (2), since conditioning on `Employed==1` is no longer a concern once the outcome itself is hours. Don't conflate the two exposure measures across the two DDDs.

**Further update (2026-09-12):** the three occupation-level robustness DDDs described below (`run_ddd_regression()`, `main.R` §8b-8d as they existed at the time) were subsequently deprecated and removed, along with `ddd_regression.R` itself — see `docs/decisions/employment-ddd-robustness-removal.md` (deleted 2026-09-24, in git at `c800efe^`). The secondary DDD's own two-spec cell-based regression (this memo's actual "Decision") is unaffected; only its occupation-level robustness variants were removed. The primary (hours) DDD retains its own occupation-level robustness variants (calibrated/external/realized), which reuse `hours_ddd_regression.R`'s `run_hours_ddd_regression()` directly rather than the now-removed file.

## The problem

Checkpoint 6/7 (`docs/ROADMAP.md`) built a single realized-WFH occupation index (`wfh_exposure_index.R`'s `build_wfh_exposure_index()`, anchored to 2021 per `docs/decisions/checkpoint6-wfh-anchor-year.md`) and fed it directly into the DDD mechanism regression (`ddd_regression.R`'s `run_ddd_regression()`). Two problems surfaced with using that index — or the external Dingel & Neiman teleworkability score alone — as the *primary* DDD regressor:

1. **Post-treatment contamination.** A purely realized (2021, or later 2022-23) WFH index is measured *after* the treatment period begins. Feeding it into the DDD as if it were an exogenous exposure measure risks the exposure regressor itself absorbing part of the treatment effect it's supposed to explain.
2. **Small-cell instability.** An earlier version of the pipeline combined the external and realized measures by swapping to realized values wherever the raw gap between them exceeded an arbitrary threshold, with no account of sampling noise. This let a 4-observation occupation cell (ISCO 63, subsistence farmers) swing the ranking on the strength of a single person surveyed 4 times in one year — the gap looked large (0.000 → 0.750) but was statistically indistinguishable from noise given how few observations backed it.
3. **Both problems compound in the DDD's exposure regressor specifically**, since that's the variable the paper's causal claim rests on.

## Options considered

**A. Keep a single occupation-level index (Checkpoint 6/7 as originally scoped), accept both problems.** Simplest, but leaves the DDD's central regressor exposed to exactly the two risks above with no mitigation.

**B. Statistically calibrate the swap decision; keep the DDD occupation-level.** Fixes problem 2 (small-cell instability) via a principled test, but not problem 1 (the calibration still draws on 2022-23 realized data, so it's a documented compromise rather than a clean pre-treatment measure) or the third-difference's implicit conditioning on `Employed` (an occupation code only exists for the employed, so an occupation-level DDD regressor structurally excludes non-employed rows from the third difference).

**C. Calibrate the swap decision (fixing problem 2), AND move the DDD's primary regressor to a pre-period (2017-2019) demographic-cell shift-share measure built from the calibrated occupation scores (mitigating problem 1 and the employment-conditioning issue).** Four separate exposure measures are kept, each reported, rather than collapsed into one "best" index — see the Decision below for why.

## Decision

**Option C.** Four exposure measures are built, each for a different purpose, and none is treated as a strict replacement for the others:

1. **External** (`build_exposure_isco2()`, `wfh_exposure_cells.R`) — the raw Dingel & Neiman teleworkability score via an O\*NET/SOC→ISCO crosswalk. Pre-period by construction (a US-task-based measure, immune to Israel's own COVID-era WFH behavior), but misclassifies occupations where Israeli institutional practice diverges sharply from the US task-content prediction — teaching (ISCO 23) is the clear case: D&N scores it near-ceiling teleworkable (0.966), but Israeli schools stayed in-person by Ministry of Education policy, and realized 2022-23 usage among Israeli teachers is 0.063.

2. **Calibrated** (`calibrate_isco_exposure()`, `wfh_exposure_cells.R`) — corrects the external score only where the realized-vs-external gap is large *and* well-powered enough that it can't plausibly be sampling noise: a one-sided, cluster-robust test (`cluster = ~IDPUF`, appropriate here since the test is fit *within* one occupation's own rows, where the same respondent can recur across survey waves) of whether the true gap exceeds a substantive threshold (`gap_threshold = 0.5`) by more than the realized estimate's own sampling uncertainty, at `conf_level` confidence (default 95%). Occupations too thin to compute a cluster-robust SE at all (e.g. ISCO 63 — its 4 rows all belong to one `IDPUF`, so `feols()` reports "infinite or missing values") fail the test automatically and keep their theoretical value — the same outcome a flat sample-size floor would give, but derived from the estimator's own precision rather than a second, independently-chosen number. Still draws on 2022-23 realized data internally, so this remains a documented compromise, not a fully pre-treatment measure — reported alongside (3) and the primary measure (4) specifically so the paper can show whether conclusions depend on it.

   As of the 2026-09-09 audit fix, this and measure (3) are built from `exposure_population_df` (`cleaned_df` + a men's frame loaded via `load_and_clean_data(folder_path, sex_filter = "men")`) rather than `cleaned_df` alone — see `wfh_exposure_index.R`'s header comment. `cleaned_df` (women 25-59) is the exact population that later enters the primary DDD as `Mother`/`Post`/`Employed`; building an occupation's realized/calibrated exposure score from that same population risked a mechanical link between the regressor and the outcome-adjacent population, independent of any true exposure channel. Adding men breaks that identity for these two occupation-level aggregators (neither conditions on sex). `build_exposure_cells()` (4) is unaffected by this change — it already stratifies by `Min` as a cell variable, so its women-cell output is identical either way.

3. **Realized** (`build_wfh_exposure_index()`, `wfh_exposure_index.R`) — the original Checkpoint 6 occupation index, 2021-anchored, `min_n = 200` to drop occupations too thin to trust. Post-treatment by construction — reported as a robustness check, not the primary measure.

4. **Cell-based shift-share** (`build_exposure_cells()`, `wfh_exposure_cells.R`) — **the primary DDD regressor.** Built entirely from **pre-period (2017-2019)** rows: each demographic cell (`Min × GilNK × TeudaGvoha × MachozMegurim`) gets a weighted average (by `MishkalSofi`) of the calibrated occupation exposure score (2), weighted by that cell's own pre-period occupational composition. Unlike (1)-(3), this is defined for **every row of `cleaned_df`, employed and non-employed alike** — it's the only one of the four that doesn't condition the DDD's third difference on `Employed`, the regression's own outcome variable.

**The secondary DDD** (`main.R` §8b, originally built and described here as "the primary DDD" before the hours pivot demoted it — see the update note at the top of this memo) regresses `Employed ~ Mother*Post*WFH_Exposure + controls` using measure (4), in two specs. **Update (see `docs/decisions/exposure-cell-granularity-fix.md`):** measure (4)'s own cell definition (`exposure_cell_vars`) is now deliberately finer than `cell_fe_vars` below — it additionally varies by `MatzavMishpachti` and `Dat` (both already `DEFAULT_CONTROLS`, both pre-period demographic variables defined for everyone regardless of employment status). This was found to matter: when the two were identical (the original design, described below for history), this DDD's minimum detectable effect for `Mother:Post:WFH_Exposure` was ~51% of the baseline employment rate — the null result was uninformative, not evidence of a true null. Decoupling the two cut that MDE by ~37%.
- **Spec 1 (additive controls):** `cell_fe_vars = (GilNK, TeudaGvoha, MachozMegurim)` — three of the five `DEFAULT_CONTROLS` terms — are included additively. `WFH_Exposure` still carries some overlap with these three (it's built partly from them), quantified at runtime by `ddd_collinearity_diagnostics.R`'s `check_spec1_collinearity()` (R², VIF, design-matrix condition number) rather than a static comment, but no longer overlaps completely now that `exposure_cell_vars` also varies by `MatzavMishpachti`/`Dat`.
- **Spec 2 (interacted cell fixed effects):** fully interacted cell FE (`GilNK^TeudaGvoha^MachozMegurim`) no longer span the same partition `WFH_Exposure` was built on, so `WFH_Exposure`'s bare main effect is NOT dropped by collinearity here (confirmed against real data) — unlike the original design (below), where `exposure_cell_vars` and `cell_fe_vars` were identical and `WFH_Exposure`'s main effect was exactly collinear with the FE and auto-dropped by `fixest`. The triple interaction remains identified either way, since `Mother` and `Post` vary within a cell even though `WFH_Exposure`'s own cross-cell-vs-`cell_fe_vars` component doesn't.

*(Original design, 2026-09-09 through 2026-09-11, kept for history: `exposure_cell_vars` was identical to `cell_fe_vars` — `WFH_Exposure` was built from exactly `(GilNK, TeudaGvoha, MachozMegurim)`, so it was constant within every `cell_fe_vars` cell. Spec 1's `WFH_Exposure` therefore had substantial overlap with its own controls, and Spec 2's fully interacted FE absorbed `WFH_Exposure`'s cross-cell level entirely, exactly collinear with the FE. See `docs/decisions/exposure-cell-granularity-fix.md` (B1/B2 section) for the real-data diagnosis of why this made the design severely underpowered.)*

Both specs also include `Mother:GilNK` (added 2026-09-11, see `docs/decisions/age-balance-robustness-chain.md`) — a real, confirmed `GilNK` imbalance between `Mother==1`/`0` in the pre-period, whose size varies by `WFH_Exposure` quartile, that the purely-additive `GilNK` control couldn't correct for. Confirmed against real data not to change the `Mother:Post:WFH_Exposure` conclusion (stays insignificant either way) — it's included because it surfaces its own real finding (an age-heterogeneous motherhood penalty), not because it rescues the WFH-mechanism result.

Both specs cluster SEs on the `(GilNK, TeudaGvoha, MachozMegurim)` cell itself, not `IDPUF` (as of the 2026-09-09 audit fix). `WFH_Exposure` is constant within a cell, so individual-level clustering doesn't see the correlation a shared exposure value and shared unobserved cell shocks induce across individuals in the same cell — a Moulton problem that understates the SE on every `WFH_Exposure` interaction, including `Mother:Post:WFH_Exposure`, the coefficient this whole design exists to estimate. The same fix applied to `run_ddd_regression()`'s occupation-level robustness DDDs, clustered on occupation code instead. (Both that function and its `ddd_regression.R` file have since been removed — see `docs/decisions/employment-ddd-robustness-removal.md` (deleted 2026-09-24, in git at `c800efe^`). The clustering reasoning still holds for the hours DDD, which clusters on occupation for exactly this reason.) ~210 cells is above the usual >=40-50 rule of thumb for asymptotic cluster-robust inference but not large; a small-cluster correction (e.g. wild-cluster bootstrap) would need a new dependency (`fwildclusterboot`) and is a follow-up, not part of this fix.

**The three occupation-level DDDs** (`run_ddd_regression()`, called once each with measures (2), (1), and (3) — `main.R` §8b-8d as originally built) existed as robustness checks, not the primary result, precisely because an occupation-level regressor structurally excludes non-employed rows from the third difference. **These were removed 2026-09-12** — see the update note above and `docs/decisions/employment-ddd-robustness-removal.md` (deleted 2026-09-24, in git at `c800efe^`).

**Why not collapse to one index?** Doing so was the earlier version's actual mistake (problem 2 above) — a single combined index hides exactly the sensitivity (to the calibration threshold, to the pre-/post-treatment measurement choice, to employment-conditioning) that reporting all four, side by side, makes visible instead.
